const URL_REST = 'http://127.0.0.1:3001';
const URI_STREAM ='http://127.0.0.1:8890'

const account = {
  URL: URL_REST+'/accounts'
}

const stream = {
  URI: URI_STREAM
}

const config = {
  ACCOUNT: account,
  STREAM: stream
}

export default config;

